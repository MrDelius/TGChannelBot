# 🤖 TGChannelBot — Professional Telegram CMS & Management System

**TGChannelBot** — это высокопроизводительная асинхронная система управления контентом для Telegram-каналов, построенная на базе `aiogram 3.x`. Проект реализует промышленную архитектуру разграничения прав доступа (RBAC), интеллектуальные алгоритмы публикации и систему автоматического восстановления при ошибках парсинга.

🚀 **Развернуто в:** Google Cloud Platform (GCP)  
🐳 **Инфраструктура:** Docker & Docker Compose  
📂 **Архитектура:** Модульный монолит с реляционной базой данных (Many-to-Many)

---

## 🛠 Технологический стек

*   **Core:** Python 3.10+ (Asyncio)
*   **Framework:** [Aiogram 3.x](https://github.com/aiogram/aiogram) (Dispatcher, Routers, FSM)
*   **Database:** SQLite 3 (Реляционная модель с поддержкой каскадных удалений)
*   **DevOps:** Docker, Docker Compose
*   **Cloud:** Google Cloud Engine (Compute Engine)

---

## ✨ Ключевые инженерные решения

### 1. Гибридная система ACL (Access Control List)
В отличие от стандартных решений, бот использует архитектуру **Многие-ко-Многим** (Junction Table), что позволяет:
*   Связывать одного пользователя с неограниченным количеством каналов.
*   Реализовывать роли `Владелец` (полный доступ + управление админами) и `Администратор` (только публикация).
*   **Real-time Security Sync:** Каждое действие проходит верификацию через API Telegram (`get_chat_member`). При лишении прав в Telegram — доступ в боте аннулируется мгновенно.

### 2. Интеллектуальный конструктор контента
*   **7-этапный FSM-сценарий:** Пошаговый сбор данных для генерации структурированных шаблонов (Title, Subtitle, Body, Quote, Hashtags, Links).
*   **Smart HTML Detection:** Бот автоматически дифференцирует "сырой" HTML-код (паст из шаблона) и ручное форматирование Telegram, выбирая корректный `parse_mode`.
*   **Fail-Safe Publishing:** Многоуровневая система отката. Если текст содержит синтаксические ошибки HTML или канал не поддерживает Premium-эмодзи (Boost < 2), бот автоматически корректирует разметку и отправляет пост без потери данных.

### 3. Оптимизация UX и Ресурсов
*   **MediaGroup Engine:** Автоматическая сборка альбомов до 10 файлов с проброской форматирования в первый элемент группы.
*   **Интерфейсная гигиена:** Бот автоматически удаляет устаревшие сообщения с кнопками в процессе создания поста, предотвращая повторные нажатия и ошибки сессии.
*   **Cross-Platform Copy:** Генерация кликабельных `<code>` блоков с инструкциями для мобильных и десктопных версий Telegram.

---

## 📂 Структура проекта

```text
├── handlers/           # Бизнес-логика (Common, Content, Template handlers)
├── keyboards/          # Презентационный слой (Инлайн и Реплай интерфейсы)
├── states/             # Машины состояний (Finite State Machines)
├── utils/              # Слой доступа к данным (Database Access Object)
├── config.py           # Валидация окружения и типизация настроек
└── main.py             # Точка входа и оркестрация роутеров
```

---

## 🚀 Быстрый запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/MrDelius/TGChannelBot.git
    cd TGChannelBot
    ```

2.  **Настройте среду окружения:**
    ```bash
    cp .env.example .env
    ```
    Отредактируйте файл `.env`, указав ваш `BOT_TOKEN` и `ADMIN_ID` (главный администратор системы).


3.  **Запустите через Docker Compose:**
    ```bash
    docker-compose up -d --build
    ```

---

## 📜 Документация
К проекту прилагается полная **Техническая документация (Technical Design Document)**, описывающая каждый хендлер, схему базы данных, реестр методов и алгоритмические условия.

---

## 📄 Лицензия
Проект распространяется под лицензией **MIT** [LICENSE](LICENSE). Разработано для профессионального использования в медиа-сфере.

---
**Разработчик:** [MrDelius](https://github.com/MrDelius)