# 🤖 TGChannelBot — Профессиональная CMS для Telegram-каналов

**TGChannelBot** — это высокопроизводительный асинхронный инструмент на базе `aiogram 3.x`, предназначенный для централизованного управления публикациями. Исходный код бота поставляется в виде готового Docker-образа для максимально простого и надежного развертывания.

## ✨ Ключевые возможности

*   **Мультипользовательская архитектура (Many-to-Many):** Поддержка неограниченного количества каналов и администраторов в рамках одной инстанции бота.
*   **Гибкая ролевая модель:** 
    *   `Владелец`: Полный контроль над каналом и возможность принудительной синхронизации списка персонала.
    *   `Администратор`: Безопасная публикация в разрешенные медиа-ресурсы без доступа к управлению правами.
*   **Smart ACL & Real-time Security:** Динамическая проверка прав администратора через API Telegram перед каждой операцией выбора канала.
*   **Интеллектуальный конструктор шаблонов:** Пошаговый генератор HTML-разметки для создания профессионально оформленных постов (7 этапов сбора данных).
*   **Система Smart Fallback:** Автоматическое исправление синтаксических ошибок HTML и адаптация футера под уровень Boost канала (поддержка Premium-эмодзи).

## 🚀 Быстрый запуск (Docker Compose)

Это единственный поддерживаемый способ установки. Вам не нужно устанавливать Python или зависимости — Docker автоматически загрузит готовый образ и запустит бота.

1.  **Клонируйте репозиторий с конфигурацией:**
    ```bash
    git clone https://github.com/MrDelius/TGChannelBot.git
    cd TGChannelBot
    ```

2.  **Настройте среду окружения:**
    ```bash
    cp .env.example .env
    ```
    Отредактируйте файл `.env`, указав ваш `BOT_TOKEN` и `ADMIN_ID` (главный администратор системы).

3.  **Запустите бота:**
    ```bash
    docker-compose up -d
    ```

## 📂 Структура репозитория
*   `docker-compose.yml` — Инструкция для Docker по загрузке образа и запуску контейнера.
*   `.env.example` — Шаблон настроек системы.
*   `README.md` — Данная инструкция.

> **Примечание о данных:** При первом запуске Docker автоматически создаст директорию `data/` в папке проекта. В ней будет храниться файл базы данных SQLite. Не удаляйте эту папку, чтобы не потерять список подключенных каналов и админов.

## 📖 Краткий алгоритм работы

1.  **Интеграция:** Добавьте бота в ваш канал в качестве администратора с правом **"Публикация сообщений"**.
2.  **Синхронизация:** Бот мгновенно индексирует создателя (Owner) и администраторов, создавая необходимые связи в базе данных.
3.  **Менеджмент:** Используйте кнопку `Контент` для моментальной публикации или `Шаблоны` для генерации сложной HTML-разметки для последующей вставки.
4.  **Помощь:** Команда `/info` всегда доступна для детального ознакомления с функциями и этапами создания шаблонов.

## 🛡 Безопасность и архитектура
*   **Database:** Реляционная модель данных SQLite с 3-мя связанными таблицами. Поддержка `ON DELETE CASCADE` гарантирует отсутствие "мусорных" данных при удалении каналов.
*   **Security:** Использование параметризованных SQL-запросов (защита от SQLi) и полная изоляция исходного кода внутри Docker-контейнера.

## 📄 Лицензия

Проект распространяется под лицензией **MIT**. Подробнее см. файл [LICENSE](LICENSE).


---
*Разработано для эффективного и безопасного управления медиа-ресурсами в Telegram.*